import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  arrayTask: [],
  newTask: "",
  error: "",
};

const todoSlice = createSlice({
  initialState,
  name: "todo",
  reducers: {
    
  },



});
